# h3-spark2-sage — SageAttention engine on spark-2 (fabric bind).
# Everything from h3-sage (image, backend, mods, timeouts) + the worker bind.
# Run FROM spark-2:  rack up h3-spark2-sage
. recipes/h3-sage.env
SERVE_ARGS+=(--host 192.168.100.2)   # last --host wins over launch-solo's 127.0.0.1
