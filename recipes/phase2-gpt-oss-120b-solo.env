# Phase 2, leg A — the same weights as the cluster recipe, one node, no TP.
# Exists so solo-vs-TP2 is a one-variable experiment.
MODEL=openai/gpt-oss-120b
SERVE_ARGS=(
  --gpu-memory-utilization 0.8
  --max-model-len 131072
  --max-num-seqs 4
  --async-scheduling
)
ENV_EXTRA=()
MODS=()
